# Runtime dependencies for the Sprint 4 pipeline.
#
#   pip install -r requirements.txt

plotly>=5.0          # report.py: every chart in report.html
duckdb>=0.9          # load.py: the analytical store
requests>=2.28       # extract_live.py: the Fauxnance client

# Dev only
pytest>=7.0
